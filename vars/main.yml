---

# Pathes for Ansible to store downloaded or templated data
local_ansible_data_path: /usr/local/src/ansible/data
remote_ansible_data_path: "{{ local_ansible_data_path }}"

liferay_supported_platforms:
  - Debian
